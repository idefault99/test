# Ищем кто именно запросил удаление namespace — будет user-agent не равный kubelite
sudo journalctl -u snap.microk8s.daemon-kubelite --since "3 days ago" \
  | grep "dev-devops" \
  | grep -v "kubelite.*kubernetes\|namespace_controller\|csi_attacher" \
  | grep -iE "delete|namespace" \
  | head -20
