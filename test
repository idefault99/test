# Скопировать на ноды
for node in coparking-5 coparking-6 coparking-7 coparking-8 coparking-9; do
  scp calico-images.tar ikbelykh@${node}:/tmp/
done

# На каждой ноде импортировать
sudo ctr -n k8s.io images import /tmp/calico-images.tar
