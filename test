# Проверяем что токен/пароль вообще правильный
curl -u "your_user:your_token" \
  "https://registry.test/v2/dpm/infra/tags/list"
# Должно вернуть JSON со списком тегов
# Если 401/403 — проблема в самом токене
