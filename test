deploy_host_test:
  stage: deploy
  tags:
    - docker
  image: alpine:3.19
  variables:
    IMAGE_POSTFIX: ".bin"
  before_script:
    - apk add --no-cache ansible sshpass openssh-client
    - export TAG=$(eval $TAG_SCRIPT)
    - mkdir -p ~/.ssh
    - echo "StrictHostKeyChecking no" >> ~/.ssh/config
  script:
    - echo "==> Testing deploy for image ${CI_REGISTRY_IMAGE}:${TAG}${IMAGE_POSTFIX}"
    - |
      ansible-playbook ansible/deploy-test.yml \
        -i "${DEPLOY_HOST}," \
        -e "deploy_image=${CI_REGISTRY_IMAGE}" \
        -e "deploy_tag=${TAG}${IMAGE_POSTFIX}" \
        -e "registry_user=${CI_REGISTRY_USER}" \
        -e "registry_password=${CI_REGISTRY_PASSWORD}" \
        -e "registry_host=${CI_REGISTRY}" \
        -e "ansible_user=${DEPLOY_USER}" \
        -e "ansible_password=${DEPLOY_PASSWORD}" \
        -e "ansible_become_password=${DEPLOY_PASSWORD}" \
        -e "ansible_connection=ssh"
  when: manual
  rules:
    - if: $CI_COMMIT_TAG
      when: manual
    - if: $CI_OPEN_MERGE_REQUESTS
      when: manual
    - if: $CI_COMMIT_REF_NAME =~ /^(main|master|develop|release\/.*)$/
      when: manual
    - if: $CI_COMMIT_REF_NAME !~ /^(main|master|develop|release\/.*)$/
      when: manual
