Local file `pipeline/backup-probegi.yml` does not exist!

# BACKUP
trigger-probegi-backup:
  stage: backup
  when: manual
  trigger:
    include:
      - local: pipeline/backup-probegi.yml
    strategy: depend
  rules:
    - when: manual
