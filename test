NS="<твой namespace>"

# Статус джобы
kubectl get jobs -n $NS | grep redis
kubectl describe job redis-job-* -n $NS 2>/dev/null | tail -20

# Логи джобы
kubectl logs -n $NS -l app=redis-init 2>/dev/null
kubectl logs -n $NS -l app=redis 2>/dev/null | grep -v "^$" | tail -30
