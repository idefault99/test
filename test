# Сколько подов на каждой ноде
kubectl get pods -A -o wide --no-headers | awk '{print $8}' | sort | uniq -c | sort -rn

# Подробно по каждой ноде
kubectl describe nodes | grep -E "Name:|pods:" | paste - -
