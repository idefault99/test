kubectl run redis-uid-check \
  --rm -it \
  --restart=Never \
  --image=redis:7-alpine \
  -- sh -c '
    id
    whoami || true
    grep -E "^(redis|root):" /etc/passwd || true
    cat /proc/self/status | grep -E "^(Uid|Gid):"
  '