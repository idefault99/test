Привет. То, что прислали (.pem с CN=test), для Ingress использовать нельзя — это корневой CA-сертификат (самоподписанный: Issuer = Subject = test, стоит CA:TRUE, Key Usage = Certificate Sign). Он предназначен подписывать сертификаты, а не обслуживать HTTPS. Как серверный он не подойдёт по двум причинам: в нём нет нашего домена в SAN (клиент отвергнет по имени) и это CA, а не leaf-серт.

Нужен серверный (leaf) сертификат на наш домен. Пришлите, пожалуйста, в формате PEM:

	1.	Серверный сертификат на hostname: <укажи свои домены> — с этими именами в SAN (SubjectAltName), с назначением serverAuth, CA:FALSE.
	2.	Fullchain — в одном файле: наш серт + все промежуточные CA (и корневой, если цепочка от него).
	3.	Приватный ключ к этому серверному серту (PEM, без пароля) — только если CSR генерите вы. Если пришлёте только требования — я сам сгенерю ключ и CSR у себя и передам вам только CSR на подпись (так безопаснее, приватный ключ не покидает наш контур).

Не подойдёт: только корневой CA; сертификат без SAN с доменом; формат PFX/DER (нужен PEM); ключ под паролем.