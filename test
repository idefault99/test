for node in coparking-5 coparking-6 coparking-7 coparking-8 coparking-9 coparking-10; do
  echo "=== $node ==="
  ssh ikbelykh@$node sudo bash << 'EOF'
    # 1. Создать hosts.toml с skip_verify
    mkdir -p /etc/containerd/certs.d/registry.test.model.svo.aero
    cat > /etc/containerd/certs.d/registry.test.model.svo.aero/hosts.toml << 'TOML'
server = "https://registry.test.model.svo.aero"

[host."https://registry.test.model.svo.aero"]
  capabilities = ["pull", "resolve", "push"]
  skip_verify = true
TOML

    # 2. Добавить config_path если отсутствует
    if ! grep -q 'config_path' /etc/containerd/config.toml; then
      sed -i '/^\[plugins\."io\.containerd\.grpc\.v1\.cri"\]$/a\  config_path = "/etc/containerd/certs.d"' \
        /etc/containerd/config.toml
      echo "config_path добавлен"
    else
      echo "config_path уже есть"
    fi

    # 3. Перезапустить
    systemctl restart containerd
    sleep 3
    systemctl is-active containerd
    # 4. Проверить
    grep config_path /etc/containerd/config.toml
EOF
done
---
kubectl delete pod test-dns
ansible-playbook -i inventory.ini playbooks/02-registry.yml
---
# Проверить что credentials тоже работают (не только TLS)
kubectl run test2 --image=registry.test.model.svo.aero/dpm/iac/infra/alpine-netutils:1.0.0 \
  --restart=Never -- sleep 30
kubectl get pod test2 -w
