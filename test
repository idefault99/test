# Один образ
ctr -n k8s.io images import /путь/к/образ.tar.gz

# Все .tar.gz разом
for f in /путь/*.tar.gz; do
  echo "=== Импорт: $f ==="
  ctr -n k8s.io images import "$f"
done

# Проверка что всё появилось
ctr -n k8s.io images ls
