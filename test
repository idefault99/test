# --- Проверка шага 2.4 ---

# 1. Диск смонтирован
df -hT /var/lib/longhorn
# Ожидаем: xfs, ~18TB

# 2. fstab прописан
grep longhorn /etc/fstab

# 3. SELinux контекст
ls -dZ /var/lib/longhorn
# Ожидаем: container_file_t

# 4. iSCSI активен
systemctl is-active iscsid
cat /etc/iscsi/initiatorname.iscsi

# 5. crictl pull с обоих registry (замени на реальные образы)
crictl pull <harbor-host>/library/pause:3.10
crictl pull <gitlab-registry-host>/project/image:tag
