# В StatefulSet команда запуска сейчас выглядит как?
kubectl exec -n test-gis-svo redis-deployment-0 -- redis-cli config get appendonly
kubectl exec -n test-gis-svo redis-deployment-0 -- redis-cli config get save
