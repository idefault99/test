MONGO_URI = (
    "mongodb://user:password@"
    "mongodb-service.mongo-namespace.svc.cluster.local:27017"
    "/mydb"
    "?authSource=admin"
    "&replicaSet=rs0"          # обязательно для changeStream
    "&directConnection=true"   # для single-node RS — без этого драйвер может зависнуть
)
