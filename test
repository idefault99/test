Я добавил в делоймент приложения
        envFrom:
          {{- if .Values.vault.enabled }}
          - secretRef:
              name: {{ $svc.secretName }}
              optional: true
          {{- end }}
но в кластере он не появился, либо деплоймент не обновляется, либо на момент старта секрета еще не было? eso не успевает спуллить секрет?