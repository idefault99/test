cat > /tmp/kubevip-superadmin.patch << 'PATCH'
--- a/playbooks/03-k8s-cluster.yml
+++ b/playbooks/03-k8s-cluster.yml
@@ -110,16 +110,13 @@
     - name: Сгенерировать kube-vip манифест
       shell: |
         ctr -n k8s.io run --rm --net-host \
           {{ local_registry }}/kube-vip/kube-vip:{{ kubevip_version }} kv \
           /kube-vip manifest pod \
           --interface {{ vip_interface }} \
           --address {{ vip }} \
           --controlplane \
           --arp \
           --leaderElection \
-        > /etc/kubernetes/manifests/kube-vip.yaml
+        > /tmp/kube-vip-raw.yaml && \
+        sed 's|path: /etc/kubernetes/admin\.conf|path: /etc/kubernetes/super-admin.conf|g' \
+          /tmp/kube-vip-raw.yaml > /etc/kubernetes/manifests/kube-vip.yaml
       args:
         creates: /etc/kubernetes/manifests/kube-vip.yaml
-
-    - name: "Патч kube-vip → super-admin.conf (hack для К8s ≥1.29)"
-      replace:
-        path: /etc/kubernetes/manifests/kube-vip.yaml
-        regexp: 'path: /etc/kubernetes/admin\.conf'
-        replace: 'path: /etc/kubernetes/super-admin.conf'
-      when: not kubevip_exists.stat.exists
PATCH

patch -p1 < /tmp/kubevip-superadmin.patch
