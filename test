tcpdump -i eth0 -n src host 192.168.1.50 and port 443

# В одном терминале — дамп с временными метками
tcpdump -i eth0 -n port 443 -tttt

# В другом терминале — curl с твоего клиента
curl -v https://gitlab.example.com
