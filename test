deactivate

---
# Войти в venv (нужно каждый раз в новом терминале, если не добавлено в .bashrc)
source ~/.ansible-venv/bin/activate

# Убедиться что используется нужный ansible
which ansible
# → /home/user/.ansible-venv/bin/ansible

# Выйти из venv
deactivate
