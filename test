# Скачать архив репо (папка Helm)
curl -s --header "PRIVATE-TOKEN: <token>" \
  "https://gitlab.example.com/api/v4/projects/<project_id>/repository/archive.tar.gz?path=Helm" \
  -o /tmp/helm-chart.tar.gz

tar -tzf /tmp/helm-chart.tar.gz | head -20
