microk8s kubectl apply -f - <<EOF
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: external-secrets-tokenreview
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:auth-delegator
subjects:
  - kind: ServiceAccount
    name: external-secrets
    namespace: external-secrets
EOF


microk8s kubectl auth can-i create tokenreviews \
  --as=system:serviceaccount:external-secrets:external-secrets
# Должно быть: yes

# Через 15 сек
microk8s kubectl get clustersecretstore vault-cluster-store-dev-devops
