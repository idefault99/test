kubectl exec -n develop redis-deployment-0 -- redis-server --version
kubectl exec -n develop redis-deployment-0 -- redis-cli ping
# И покажи чем запускается контейнер:
kubectl exec -n develop redis-deployment-0 -- cat /etc/redis/redis.conf 2>/dev/null || \
kubectl exec -n develop redis-deployment-0 -- ps aux | grep redis
