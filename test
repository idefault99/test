-- Проверить настройки на уровне БД и пользователя
SELECT rolname, rolconfig
FROM pg_roles
WHERE rolconfig IS NOT NULL;

-- Проверить настройки на уровне конкретной БД
SELECT datname, datconfig
FROM pg_database
WHERE datconfig IS NOT NULL;
