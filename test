kubectl exec -n test-gis-svo redis-deployment-0 -- sh -c '
  echo "=== /data/ ===" && ls -la /data/
  echo "=== df ===" && df -h /data
  echo "=== config ===" && redis-cli config get dir && redis-cli config get appendonly && redis-cli config get appendfilename
'
