# На Vault VM — проверить доступность K8s API
curl -sk https://192.168.0.132:16443/healthz

# Проверить что SA токен валиден
SA_TOKEN=$(ssh user@k8s-node "microk8s kubectl get secret external-secrets-token \
  -n external-secrets -o jsonpath='{.data.token}' | base64 -d")

curl -sk https://192.168.0.132:16443/api/v1/namespaces/external-secrets/serviceaccounts/external-secrets \
  -H "Authorization: Bearer ${SA_TOKEN}"


microk8s kubectl auth can-i create tokenreviews \
  --as=system:serviceaccount:external-secrets:external-secrets
