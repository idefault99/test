Может быть в values.yaml вложенная структура, например:
iamges:
  main: ...
  migrations: ...
Нужно это учитывать и выводить
Если в app есть папка templates, в ней нужно только смотреть есть ли deployment.yaml, если нет, смотреть есть ли job.yaml, если есть, выводить что это джоба (Возможно есть оба файла, тогда также писать что есть job и deployment)
директорию с inventory не обрабатывать