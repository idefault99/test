cat > /etc/containerd/certs.d/registry.test/hosts.toml <<'EOF'
server = "https://registry.test"

[host."https://registry.test"]
  capabilities = ["pull", "resolve", "push"]
  [host."https://registry.test".auth]
    username = "your_gitlab_user"
    password = "your_gitlab_token_or_password"
EOF
