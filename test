# ❌ Не поддерживает cluster — нужно менять или оставить type: single
import redis
client = redis.Redis(host="redis-service", port=6379)
client = redis.StrictRedis(host="redis-service", port=6379)

# ❌ Через URL — тоже не cluster-aware
client = redis.from_url("redis://redis-service:6379")

# ✅ Поддерживает cluster — можно деплоить type: cluster
from redis.cluster import RedisCluster
client = RedisCluster(host="redis-service", port=6379)
