sudo microk8s ctr -n k8s.io tasks ls | grep dashboard

# Взять TASK ID из предыдущей команды
sudo microk8s ctr -n k8s.io tasks kill -s SIGKILL <TASK_ID>


# Найти PID
ps aux | grep dashboard | grep -v grep

# Убить
sudo kill -9 <PID>
