Сертификат в формате PEM (fullchain: leaf + промежуточные CA в одном файле).
	2.	Приватный ключ в PEM, без пароля.
	3.	Если CA внутренний — ещё root CA (ca.crt).
Домены/SAN: <перечисли hostname, что пойдут в Ingress>. Не PFX/DER, а PEM.