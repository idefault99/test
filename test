- name: Remove old probegi dumps
  shell: |
    find /tmp -maxdepth 1 -type f -name "probegi_*.sql.gz" -delete
  args:
    executable: /bin/bash
  tags:
    - cleanup_dumps
    
script:
  - ansible-playbook -i infra/ansible/inventory.yml infra/ansible/backup/backup.yml --tags cleanup_dumps
  
  