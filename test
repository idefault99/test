docker run --rm \
  -v "$PWD:/work" \
  eclipse-temurin:17-jre \
  sh -c '
    keytool -importcert -noprompt -trustcacerts \
      -alias sogaz-rabbitmq-ca \
      -file /work/sogaz-bundle.crt \
      -keystore /work/truststore.p12 \
      -storetype PKCS12 \
      -storepass changeit
  '
