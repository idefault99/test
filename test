# 1. Отключить dashboard — прямо сейчас
sudo microk8s disable dashboard

# 2. Подождать 2-3 минуты, посмотреть стало ли лучше
sudo journalctl -u snap.microk8s.daemon-kubelite -f | grep -v "^.*Trace"

# 3. Проверить RAM
free -h
