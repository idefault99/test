trigger-probegi-backup:
  stage: backup
  when: manual
  trigger:
    include:
      - project: 'dpm/probegi/infra'
        ref: 'master'
        file: '/pipeline/backup.gitlab-ci.yml'
    strategy: depend
  rules:
    - when: manual